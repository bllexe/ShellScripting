# Drink Water Reminder

#### set script to crontab -e and set  every two hours like will working
#### if you want to get audio voice before you install espeak  __sudo apt-get install__ with this command
#### open crontab -e  command if you want open crontab default wit vim  firt of all you set __export VISUAL=vim__ like this. 
